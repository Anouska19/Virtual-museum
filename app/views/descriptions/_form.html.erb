<%= form_with(model: description, local: true) do |form| %>
  <% if description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(description.errors.count, "error") %> prohibited this description from being saved:</h2>

      <ul>
      <% description.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :technique_utilise %>
    <%= form.text_field :technique_utilise %>
  </div>

  <div class="field">
    <%= form.label :l_histoire_de_l_oeuvre %>
    <%= form.text_field :l_histoire_de_l_oeuvre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
